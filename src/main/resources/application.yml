spring:
  datasource:
    url: jdbc:mysql://localhost:3306/addressbook
    username: ${db_user}
    password: ${db_pass}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update